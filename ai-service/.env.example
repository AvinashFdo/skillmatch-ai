# Copy to .env. Same MongoDB Atlas connection string used by the
# backend (backend/.env) - role data now lives in the shared `roles`
# collection, read here directly via pymongo rather than through the
# backend's API, to avoid a circular HTTP dependency between the two
# services.
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/skillmatch
