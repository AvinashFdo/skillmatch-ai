# Copy to .env. Leave unset (or set to the localhost URL) for normal
# local development. Set to the backend's public tunnel URL only during
# a remote user-testing session - see backend/README.md /
# CLAUDE_LOG.md for the full tunnel setup steps.
VITE_API_BASE_URL=http://localhost:5000/api
